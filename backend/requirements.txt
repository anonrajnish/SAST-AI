# Runtime dependencies (Python 3.12). Foundation scope only — see docs/TASK_BACKLOG.md.
# Deferred (added with their feature task): celery, redis client, cryptography, boto3, provider SDKs.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
structlog==24.4.0
